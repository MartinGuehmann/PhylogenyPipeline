# Resource requests for scheduler job scripts, one line per script.
# Edit this file to tune resources for a particular cluster instead of
# editing every job script individually. Scheduler-Sub.sh reads it and
# passes the values to qsub (-l select=...) or sbatch (--cpus-per-task,
# --mem, --time), whichever is used to submit the job.
#
# script                                                ncpus  mem_gb  walltime  partition
#
# partition is a comma-separated list of Slurm partitions the job is
# eligible for (--partition=a,b,c), or blank to stay on the cluster's
# own default partition/queue. Slurm places the job into whichever
# listed partition can start it soonest, automatically skipping any
# whose own time limit is shorter than this line's walltime - the
# closest thing Slurm has to PBS Pro's routing queues (which pick an
# execution queue from the resource request automatically, without the
# submitter naming one at all; true routing needs admin-side
# configuration - a job_submit plugin - which isn't something this repo
# can set up itself). List every partition whose own time limit
# (`sinfo`) is >= this line's walltime, among the ones meant for
# general-purpose batch compute; leave out specialised pools (this
# cluster also has fat/gpu/vis/vascunet, none listed below) unless a
# job actually needs that hardware, and leave out `interactive` too -
# despite being general-purpose hardware, it's meant for live/debug
# sessions, not batch jobs, so none of these lines list it.
#
# For PBS Pro, only a single queue name (no comma) is passed via -q;
# a comma-separated list here is simply not passed to qsub at all, so
# PBS's own default/routing queue handles placement instead - PBS has
# no equivalent of Slurm's multi-partition candidate list.
#
# AskForWholeNode is not a script name - it's a named profile that
# Scheduler-Call.sh asks Scheduler-Sub.sh for (via -R) instead of a
# script's own line, when NeedsBuilding.sh says the local Uniprot BLAST
# databases still need to be built. makeblastdb ignores its assigned CPU
# count and grabs everything on the node it lands on, so this asks for
# the whole node instead: on Slurm, Scheduler-Sub.sh turns this profile
# into `--exclusive` and ignores the ncpus/mem below entirely, so it
# auto-sizes to whatever node it lands on. PBS Pro has no equivalent
# auto-sizing, so its `-l place=excl` (also added by Scheduler-Sub.sh for
# this profile) still needs the ncpus/mem below to size the select
# statement - those are still just the old cluster's biggest node size,
# and only matter for PBS; resize them if moving to a new PBS cluster.
AskForWholeNode                                         24     187     72:00:00  standard,long
00_Scheduler-GetGenesFromAllDataBases.sh                8      32      72:00:00  standard,long
01_Scheduler-CombineHitsForEachDatabase.sh              8      8       4:00:00   standard,long
02_Scheduler-CombineHitsFromAllNCBIDatabases.sh         8      8       4:00:00   standard,long
03_Scheduler-ExtractSequences.sh                        8      8       8:00:00   standard,long
04_Scheduler-MakeNonRedundant.sh                        8      8       4:00:00   standard,long
05_Scheduler-MakeClansFile.sh                           96     16      72:00:00  standard,long
06_Scheduler-ClusterWithClans.sh                        96     251     72:00:00  standard,long
07_Scheduler-MakeTreeForPruning.sh                      1      100     24:00:00  standard,long
08_Scheduler-ExtractSequencesOfInterest.sh              8      16      4:00:00   standard,long
09_Scheduler-AlignWithClustalO.sh                       96     100     12:00:00  standard,long
09_Scheduler-AlignWithFAMSA.sh                          8      30      4:00:00   standard,long
09_Scheduler-AlignWithLINSI.sh                          96     251     24:00:00  standard,long
09_Scheduler-AlignWithMAFFT.sh                          96     100     4:00:00   standard,long
09_Scheduler-AlignWithMAGUS.sh                          8      100     24:00:00  standard,long
09_Scheduler-AlignWithMUSCLE.sh                         8      32      12:00:00  standard,long
09_Scheduler-AlignWithMUSCLE5.sh                        8      32      24:00:00  standard,long
09_Scheduler-AlignWithPASTA.sh                          4      30      72:00:00  standard,long
09_Scheduler-AlignWithRegTCoffee.sh                     96     100     72:00:00  standard,long
09_Scheduler-AlignWithSUPER5.sh                         8      32      24:00:00  standard,long
09_Scheduler-AlignWithVCMSA.sh                          4      30      72:00:00  standard,long
10_Scheduler-Long-MakeTreeWithIQ-Tree.sh                96     64      72:00:00  standard,long
10_Scheduler-MakeTreeWithIQ-Tree.sh                     8      32      24:00:00  standard,long
11_Scheduler-RemoveRogues.sh                            4      8       1:00:00   short,standard,long
12_Scheduler-ConvertTreesToFigures.sh                   1      8       1:00:00   short,standard,long
13_Scheduler-SplitNonRedundantSequences.sh              8      8       1:00:00   short,standard,long
14_Scheduler-AlignWithPASTAForPruning.sh                4      30      8:00:00   standard,long
15_Scheduler-MakeTreeWithIQ-TreeForPruning.sh           8      32      20:00:00  standard,long
16_Scheduler-ExtractSequencesOfInterest.sh              8      16      4:00:00   standard,long
17_Scheduler-SkipSequenceExtraction.sh                  1      1       0:10:00   short,standard,long
Scheduler-00-ExtractSequences.sh                        1      1       0:10:00   short,standard,long
Scheduler-01-PrepareSequences.sh                        1      1       0:10:00   short,standard,long
Scheduler-04-ContinueMakeBigSequenceFile.sh             1      1       0:10:00   short,standard,long
Scheduler-09-RogueOptAlign.sh                           1      1       0:10:00   short,standard,long
Scheduler-10-RogueOptTree.sh                            1      1       0:10:00   short,standard,long
Scheduler-11-RemoveMoreRougues.sh                       1      1       0:10:00   short,standard,long
Scheduler-11a-RestoreSequenceOfInterest.sh              1      1       0:10:00   short,standard,long
Scheduler-12-RevisualizeAllTrees.sh                     1      1       0:10:00   short,standard,long
Scheduler-13-ExtractSequencePreparation.sh              1      1       0:10:00   short,standard,long
Scheduler-14-ExtractSequencesOfInterestWithPASTA.sh     1      1       0:10:00   short,standard,long
Scheduler-15-ExtractSequencesOfInterestWithIQ-Tree.sh   1      1       0:10:00   short,standard,long
Scheduler-16-TreeBuildScheduler.sh                      1      1       0:10:00   short,standard,long
Scheduler-Call.sh                                       1      1       0:10:00   short,standard,long
